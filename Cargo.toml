[package]
name = "fscript-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

# [profile.test] # fixed
# opt-level = 2

[features]
default = ["perf"]
perf = []
